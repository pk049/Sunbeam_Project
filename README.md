The MCP_servers/ Contains all the actual MCP server implementations.

The Helper_functions/ Contains the functions to manage the session history.

Run the app.py then Our server will be started automatically.
